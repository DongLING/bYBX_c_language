#include <stdio.h> 
void main() 
{
  long f1=1,f2=1,f3; //初始化数列的前两项：f1=1,f2=1
  int i; 
  printf(" %91d %91d",f1,f2); //输出数列的前两项，即：1 1 
  for (i=3;i<=30;i++) //循环计算数列的第3项至第30项
{
    //f1是当前项的前二项，f2是当前项的前一项，f3是当前项

    f3=f1+f2; //当前项等于前两项的和
    printf(" %91d",f3); //输出当前项的值
    f1=f2; //当前项后移，前二项等于原先的前一项
    f2=f3; //前一项等于当前项
    if (i % 5==0)printf("\n"); //如果一行已经显示了5项的值，则换行显示
  } 
}